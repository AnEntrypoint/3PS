// This file now redirects to the new static data implementation
// The original PocketBase implementation has been backed up to db.ts.backup

import dataModule from './data.js';

export default dataModule;
